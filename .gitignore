# === goaldev runtime artifacts ===
.goal_driven_parallel_dev/
docs/MEMO.txt

# === user data directories (managed by kb init in separate repos) ===
records/
config/
inbox/
.kb/

# === Python test artifacts ===
.coverage
